[phases.setup]
nixPkgs = ["nodejs_24", "pnpm"]

[phases.install]
cmds = ["cd /app && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd /app && pnpm --filter @workspace/api-server run build"]

[start]
cmd = "cd /app && pnpm --filter @workspace/api-server run start"
